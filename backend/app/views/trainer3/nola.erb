<div class="jumbotron text-center">
  <div class="container">
    <a href="/" class="logo"><img src="<%= asset_path('logo.png') %>"></a>
    <h1>Nola Murazik V</h1>
      <p>Nomad Trainer</p>
       <a type="button" class="btn btn-lg btn-primary" href=""><span class="glyphicon glyphicon-calendar"></span> Schedule</a>
         <% @coaches.find(9,10,11,12,13).each do |coach| %>
          <td><h2><%= coach.weekday %></h2><h2><%= coach.availableat %> — <%= coach.availableuntil %></h2></td>
        </tr>
      <% end %>
  </div>
</div>
